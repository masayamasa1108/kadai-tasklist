<h1>タスクリスト一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li> タスクID: <%= link_to task.id, task %>, ステータス : <%= task.status %>, タスク内容 : <%= task.content %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの登録', new_task_path %>